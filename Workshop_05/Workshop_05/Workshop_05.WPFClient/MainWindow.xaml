<Window x:Class="Workshop_05.WPFClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Workshop_05.WPFClient"
        mc:Ignorable="d"
        Title="Let's chat!" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="lb_messages" ItemsSource="{Binding Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>                           
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <!--<Label Grid.Row="0" Grid.Column="0" Content="{Binding Sender}"/>-->
                        <Label Grid.Row="0" Grid.Column="1" Content="{Binding Message}"/>
                        <!--<Label Grid.Row="1" Grid.Column="1" Content="{Binding Date}"/>-->
                    </Grid>                    
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox Grid.Row="1" x:Name="tb_message" Background="AliceBlue"/>
        <Button Grid.Row="1" Content="Send" VerticalAlignment="Center" HorizontalAlignment="Right" Height="40" Width="60" FontFamily="Times New Roman" FontSize="16" Background="LightSeaGreen" Margin="6" Command="{Binding SendCommand}"/>
    </Grid>
</Window>
